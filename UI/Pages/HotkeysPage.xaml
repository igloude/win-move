<Page
    x:Class="Tactadile.UI.Pages.HotkeysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tactadile.UI.Pages">
    <Page.Resources>
        <local:HotkeyListTemplateSelector x:Key="TemplateSelector">
            <local:HotkeyListTemplateSelector.HotkeyTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="12" Padding="4,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="4" Padding="6,2"
                                VerticalAlignment="Center"
                                Visibility="{Binding ProBadgeVisibility}"
                                Background="{StaticResource TierBadgeProBrush}">
                            <TextBlock Text="PRO" FontSize="10" FontWeight="SemiBold"
                                       Foreground="White" />
                        </Border>
                        <TextBlock Text="{Binding FriendlyName}" Grid.Column="1"
                                   VerticalAlignment="Center"
                                   Opacity="{Binding RowContentOpacity}" />
                        <TextBlock Text="{Binding ShortcutDisplay}" Grid.Column="2"
                                   VerticalAlignment="Center" FontFamily="Consolas"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Opacity="{Binding RowContentOpacity}" />
                        <TextBlock Text="{Binding ParameterSummary}" Grid.Column="3"
                                   VerticalAlignment="Center" FontFamily="Consolas" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   Opacity="{Binding RowContentOpacity}" />
                        <Button Content="Change" Grid.Column="4" Click="OnChangeClick" Tag="{Binding}"
                                IsEnabled="{Binding IsChangeEnabled}"
                                Opacity="{Binding RowContentOpacity}" />
                        <Button Content="Clear" Grid.Column="5" Click="OnClearClick" Tag="{Binding}"
                                IsEnabled="{Binding IsChangeEnabled}"
                                Opacity="{Binding RowContentOpacity}" />
                    </Grid>
                </DataTemplate>
            </local:HotkeyListTemplateSelector.HotkeyTemplate>
            <local:HotkeyListTemplateSelector.SeparatorTemplate>
                <DataTemplate>
                    <Border Padding="0,4">
                        <Border BorderThickness="0,0,0,1"
                                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" />
                    </Border>
                </DataTemplate>
            </local:HotkeyListTemplateSelector.SeparatorTemplate>
        </local:HotkeyListTemplateSelector>
    </Page.Resources>
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Hotkey Bindings" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12" />

        <ListView x:Name="HotkeyList" Grid.Row="1"
                  SelectionMode="None" IsItemClickEnabled="False"
                  ItemTemplateSelector="{StaticResource TemplateSelector}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0,4" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"
                    Spacing="8" Padding="0,12,0,0">
            <Button Content="Reset to Defaults" Click="OnResetDefaults" />
            <Button x:Name="SaveButton" Content="Save" Style="{StaticResource AccentButtonStyle}"
                    Click="OnSave" IsEnabled="False" />
        </StackPanel>
    </Grid>
</Page>
